[package]
name = "ec-executor"
version = "0.1.0"
authors = ["patract labs <https://github.com/patractlabs>"]
edition = "2018"

[dependencies]
log = "0.4.8"
codec = { package = "parity-scale-codec", version = "2.0.0" }

sp-version = { version = "3.0.0" }
sp-core = { version = "3.0.0" }

sc-executor = { version = "0.9.0" }
sc-executor-common = { version = "0.9.0" }

[dev-dependencies]
